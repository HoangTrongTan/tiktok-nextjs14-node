import classNames from "classnames/bind";
import style from './Profile.module.scss';
const cx = classNames.bind(style);
function LikedList() {
    return ( <div className={cx('box-video__list')}>
        
    </div> );
}

export default LikedList;